[phases.setup]
nixPkgs = ["ffmpeg", "libsndfile"]

[phases.install]
cmds = [
  "python --version",
  "pip install --upgrade pip",
  "pip install -r requirements.txt",
  "python -c \"import moviepy, sys; print('moviepy', moviepy.__version__)\""
]

[processes]
web = "uvicorn main:app --host 0.0.0.0 --port $PORT"
